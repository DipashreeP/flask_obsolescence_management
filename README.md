# Flask LLM Obsolescence Bot

Run locally:
```bash
pip install -r requirements.txt
python app/app.py
streamlit run dashboard/app.py
```

Deploy with Terraform:
```bash
cd terraform
terraform init
terraform apply
```